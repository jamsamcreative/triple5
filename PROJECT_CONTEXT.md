# Project Context — triple5

**Last updated:** 2026-09-18

> This is a living briefing document. Claude must revise it continuously during work — see CLAUDE.md for the rules. A Stop hook will remind you if source files are newer than this file.

---

## What This Is

The custom-code repo for **triple5.local**, the WordPress marketing site for **Triple 5 Construction LLC** — a licensed general contractor and metal roofing/siding specialist serving Spokane, WA and Coeur d'Alene, ID.

- **Local dev site:** `triple5.local` (WordPress 7.0.1 · PHP 8.2 · MySQL 8.4), managed by **Local** (Flywheel/WP Engine) at `~/Local Sites/triple5`.
- **This repo** tracks **custom code only** (theme + brand mu-plugin). WordPress core and content live in the Local site and are intentionally NOT version-controlled here.
- **Remote:** `github.com/jamsamcreative/triple5` — `main` tracks `origin/main`, pushed 2026-09-18.
- **Connection model:** real files live here in the repo and are **symlinked into the Local site**, so edits here go live immediately on `triple5.local`. See README.md.

## Current State

WordPress site being branded. Active theme is **Themeco Pro** (v6.8.11) with Cornerstone builder bundled. Brand tokens are wired in site-wide via a must-use plugin. Two commits: `02ca37e` (brand, theme, header) and `c9eff33` (2026-09-18: full element library, Quote Requests dashboard, charcoal/red recolor). All commits through the Home service-panel copy/links pushed to `origin/main` (2026-09-18). Working tree clean.

### What's Working
- **`wp-content/themes/triple5/`** — custom starter **block theme** (style.css, theme.json, functions.php, templates/index.html, parts/header+footer). Symlinked into the Local site.
- **`wp-content/mu-plugins/triple5-brand.php` + `triple5-brand/`** — must-use plugin loading the design-system fonts + tokens + a brand base layer on the front end and in the Cornerstone builder. Symlinked into the Local site. Verified enqueued + HTTP 200.
- **Native Cornerstone header** — `cs_header` **"Primary Header" (ID 26)**, built via Cornerstone's Document API. Two bars in the `top` region: (1) red top bar (contact text left, hours right); (2) **nav bar restyled 2026-09-15 to the owner's reference**: charcoal-900 `#1f1d19` field, no bottom rule, **118px tall** (`bar_height`, not padding — padding is ignored when `bar_height` is set), 36px white 555 logo (media ID 50, links home), **sentence-case Public Sans 600 links** (hover red-300), then at right a **white pill phone button** (`l-phone` icon + `(509) 251-2829`, `tel:` link) and a **red pill "Get a Free Quote →"** CTA (plain arrow on the right via `anchor_flex_direction:row-reverse`). Header `settings.customCSS` (scoped `.e26-e4`) overrides the brand base-layer button rule (which forces uppercase display type on `.x-anchor-button`) and pins the layout to measurements taken from the reference screenshot (2× retina, halved): `justify-content:flex-start`; menu `margin-left:38px; margin-right:auto`; link anchors padding 0 with `li + li{margin-left:28px}`; pills `.x-anchor-content{height:42px;padding:0 18px}` (Cornerstone's own padding/line-height made them 51px), phone pill `anchor_margin` 12px right; buttons `flex-shrink:0; white-space:nowrap`. Verified via getBoundingClientRect: bar 118, logo 36, logo→menu 38, link gaps 28, pills 42×(202/208) 12 apart. Editable in Pro → Headers. Assigned **site-wide**. Nav bound to the WP **primary** menu. Colors hard-coded hex in the header JSON (post_content of post 26) — edit via `cornerstone('Resolver')->getDocument(26)->update(['elements'=>…,'settings'=>…])->save()`; `_cs_generated_tss` regenerates on next page load. Backups: scratchpad `header-26-backup*.json`.
  - Cornerstone gotchas learned: `anchor_text_reverse` does nothing for graphic placement — use `anchor_flex_direction:'row-reverse'`; an `image` element needs `image_width/height` (intrinsic) AND fixed `image_styled_width/height` or it stretches; `nav-inline` items spread unless `menu_flex`/`menu_items_flex` = `0 0 auto`. Element defaults: `cornerstone('Elements')->get_element($type)->get_defaults()`.
- **WP menu "Primary"** (term 5): Home/About/Our Services/Projects/Contact Us, assigned to the `primary` location (Appearance → Menus). Nav pages: About (13), Our Services (14 — built, Service Tabs), Projects (15), Contact Us (16). (Menu items were edited outside this repo by 2026-09-18 — nav now shows Our Services / Projects / Locations / About / Contact Us, no Home link.)
- **Home page** (ID 10) is the static front page, on Pro template **`template-blank-5.php`** ("No Container | Header, No Footer") so Pro renders the Cornerstone header (no footer placeholder).
- **`triple5-header` mu-plugin** (coded header) — now **DISABLED** (`triple5_header_should_render()` returns false); superseded by the Cornerstone header. Kept in repo for reference/rollback. Its earlier clay **top bar** (email/phone/hours) is NOT in the Cornerstone header yet.
- **`triple5-hero` mu-plugin** — registers a **"Triple 5 Hero" Cornerstone element** (Element API, `cs_register_element('t5-hero', …)`) + lead capture. Full-bleed hero (headline w/ `*asterisk*`→clay accent, subtext, financing line, **linkable review badges** (Google/Trustpilot — optional URL control per badge → renders `<a target=_blank>`), swappable **background image** w/ navy blueprint-grid placeholder fallback, editable **lead form**). Symlinked in. **Placed live on Home (ID 10)**, top of the Cornerstone tree, below the nav — verified over HTTP.
  - **Form renders at runtime, not baked:** the element bakes an inert `<div class="t5-hero__form-mount" data-t5-form="<hex(json)>">` into post_content; a `the_content` filter (priority 20) swaps it for the live form. Necessary because Cornerstone's bake runs `do_shortcode`+KSES — shortcodes/`<form>`/`<input>` get stripped, and a baked nonce would be stale. Form posts to `admin-post.php?action=t5_lead` (nonce + honeypot + validate).
  - **Leads stored** as `t5_lead` CPT ("Leads" in wp-admin, private) with fields as post meta + `wp_mail` to a per-element recipient control (default `admin_email`). Verified: test submission stored as lead #34 (test data — clean up later). On Local, mail lands in Mailpit.
  - **Placement/re-bake API (pages = Content docs):** `cornerstone('Resolver')->getDocument(10)->updateElements($tree, get_post_meta(10,'_cornerstone_settings',true))` regenerates `_cornerstone_data` + post_content + `_cs_generated_tss`. **Do NOT use `->update([...])->save()` on a page** — that path (fine for the header, a Layout doc) blanked Home's generated TSS on 2026-09-18 (rows lost their centering CSS) until re-run through `updateElements` with the real settings meta. Scratchpad `place-hero.php`; Home CS backup at scratchpad `home-cs-backup.json`.
- **`triple5-feature` mu-plugin** — registers a reusable **"Triple 5 Feature Section" Cornerstone element** (`cs_register_element('t5-feature', …)`). Two-column image + content block from a reference screenshot, adapted to Triple 5: eyebrow (`// … //`), heading, body, **checklist (one item per line → check-circle rows)**, optional **second paragraph + CTA**, image w/ **left/right flip** + blueprint-placeholder fallback, optional **experience badge** (clay corner scoop over image), optional **clay stat card** (icon + number + label). All parts editable + individually toggleable. Symlinked in. **Demo instance placed live on the About page (ID 13)** (was empty; set to `template-blank-5.php`).
  - **Placement gotcha (important):** a bare custom element at the tree root renders EMPTY — Cornerstone drops loose top-level elements. Wrap placements in `section > layout-row > layout-column`, and **clone the exact shape of a known-good section**: the section needs `section_padding`, the `layout-row` must NOT carry `layout_row_global_container:false` (that key renders the row empty), and the element node needs `_bp_base:'4_4'` + `'_modules':[]`. See scratchpad `t5fixsvc.php` for the canonical working shape. Also, **bake as an admin user** (`wp_set_current_user(1)`) when placing programmatically so inline SVG/form markup isn't KSES-stripped (non-privileged bakes lack `unfiltered_html`).
- **`triple5-logos` mu-plugin** — registers a reusable **"Triple 5 Logo Strip" Cornerstone element** (`cs_register_element('t5-logos', …)`). Horizontal band of partner/manufacturer logos: optional heading, band background (navy/coal/stone), up to **8 logo slots** (image or shield-icon + name placeholder + optional link; empty slots skipped), and a mono/original logo image treatment (mono knocks uploaded logos to white on dark bands). Dark bands carry the faint blueprint grid. Symlinked in. **Demo placed live on Home** (order now hero → feature → services → logos). Default names are generic placeholders ("Manufacturer", "Supplier", …) — replace with real partner logos.
- **`triple5-panel` mu-plugin** — registers a reusable **"Triple 5 Service Panel"** Cornerstone element (`cs_register_element('t5-panel', …)`), built 2026-09-18 from the owner's reference screenshot. Dark charcoal-900 full-bleed band: **red intro card** (heading w/ line breaks, body, dark uppercase CTA button + URL) beside a **2×3 grid of icon tiles** (Lucide icon from a 12-icon trade set chosen via `select`, title, one-line description, optional link → tile becomes `<a>`). Empty-title tiles are skipped. Hairline tile dividers come from the grid's `gap:1px` over a translucent background. **Home instance copy rewritten 2026-09-18** (owner request): heading "One licensed crew, roof to foundation", body on metal specialty + one contractor, CTA "Get a free quote" → `/contact-us/`; tiles = Roofing / Siding / Repairs / Concrete / Remodeling / Framing, each with a one-line description and **linked to its service page** (Repairs → roofing). Linked tiles get a red title + slide-in arrow on hover (`panel.css`). **Placed live on Home at the tree ROOT** (index 1, right after the hero) — root placement is what makes the dark band full-bleed; inside `section > row > column` it is clipped to the 1200px container (the `100vw + margin-left:calc(50% - 50vw)` breakout trick misfires here — the column isn't viewport-centered). Home order now: hero → panel → feature → why → reviews → faq → services → logos.
- **`triple5-why` mu-plugin** — registers a reusable **"Triple 5 Why Choose Us"** Cornerstone element (`cs_register_element('t5-why', …)`), built 2026-09-18 from the owner's reference. Charcoal-800 full-bleed band: centered uppercase display heading + row of up to **4 reasons** (red outline-ring Lucide icon from a 12-icon set via `select`, uppercase title, body). Empty-title reasons are skipped. **Placed live on Home at the tree ROOT** (index 3, after the About/feature section). Home order now: hero → panel → feature → why → reviews → faq → services → logos.
- **`triple5-reviews` mu-plugin** — **"Triple 5 Testimonials"** Cornerstone element (`t5-reviews`) + review data layer, built 2026-09-18 from the owner's reference. Files: `sources.php` (providers **Google Places API (New)** `GET places/{id}` w/ field mask → ≤5 reviews, and **Yelp Fusion** `businesses/{id}/reviews` → 3 reviews/160-char text; normalized `{id,source,author,location,rating,text,time,url}`; transient cache w/ `cache_hours` TTL + last-good option fallback + twicedaily cron; `hidden_ids` moderation; `triple5_reviews_for_display()` applies min-rating/source/limit), `render.php` (card/badge/star markup + **`the_content` runtime re-render**: the element bakes `<div class="t5-reviews-mount" data-t5-reviews="hex(json config)">…</div>` and the filter swaps it for a fresh render each view so new/moderated reviews show without re-saving), `element.php` (controls: heading, **min star rating (default 4 → 1–3★ hidden)**, max cards, 2/3/4 columns, source toggles, badge/link toggles, **6 manual review slots** — defaults are the 4 sample reviews from the screenshot), `admin.php` (**Settings → Triple 5 Reviews**: Place ID, Google key, Yelp business ID/key (password fields, blank = keep), cache hours, Refresh now, fetched-review table with Hide/Unhide). **No API keys configured yet** — section currently shows the 4 manual samples. Placed on Home at ROOT after `t5-why`. Order: hero → panel → feature → why → reviews → faq → services → logos. Filter/moderation logic verified with simulated data; front-end verified in browser.
- **`triple5-faq` mu-plugin** — **"Triple 5 FAQ"** Cornerstone element (`t5-faq`), built 2026-09-18 from the owner's reference. White section: display heading (left, line breaks kept) + intro copy (right), then a **two-column accordion of up to 8 Q&A items** using native `<details>/<summary>` (no JS; works after bake), red CSS +/– glyph, open item tints stone-100. Controls: heading, intro, **"open by default"** picker (default Q2), 1/2 columns, **FAQPage JSON-LD toggle** (on). Empty-question slots skipped. Defaults are 6 Triple 5 Q&As drafted from the reference headings — **verify the service-area list and licensing wording with the owner.** Placed on Home at ROOT after `t5-reviews`. Order: hero → panel → feature → why → reviews → **faq** → services → logos.
- **`triple5-tabs` mu-plugin** — **"Triple 5 Service Tabs"** Cornerstone element (`t5-tabs`), built 2026-09-18 from the owner's reference; **the Our Services page (ID 14, `/our-services/`) is now built with it** (page set to `template-blank-5.php`, element at tree root). Stone page-header band (eyebrow, H1, subtext — toggleable) → underlined tab bar → one panel per service (up to 6 tabs, empty label = hidden): optional red pill badge, H2, body, **checklist (one per line, 2-col red checks)**, red pill CTA w/ white circled arrow (per-tab URL, falls back to a default CTA URL), image via `image` control w/ charcoal blueprint placeholder + mono caption. **Interaction = `tabs.js`** (enqueued, not baked): ARIA tablist, click + arrow keys, `#panel-id` deep links via `history.replaceState`; without JS all panels stack (`.is-enhanced` gates the hiding) so nothing is trapped and no `<input>`/`<button>` markup is needed. Defaults: Roofing (badge), Siding (badge), Remodeling, Concrete, Framing — **draft copy, owner to verify service scope.**
- **Individual service pages (2026-09-18)** — five child pages of Our Services, each `template-blank-5.php` with three root elements: **`t5-tabs` in single-tab mode** (bar hidden; = stone header band w/ eyebrow "Our services · X", H1, subtext + intro panel w/ badge, body, 6-item checklist, CTA → `/contact-us/`, blueprint image slot) → **`t5-why`** ("Why choose Triple 5 for X?", 4 tailored reasons) → **`t5-faq`** (4 service FAQs, Q1 open, schema on). IDs/URLs: **roofing 60** `/our-services/roofing/`, **siding 62**, **remodeling 64**, **concrete 66**, **framing 68**. Generated by `docs/scripts/build-service-pages.php` (idempotent by slug; edit content there and re-run twice, or edit in Cornerstone — re-running the script will overwrite builder edits). Home services-grid "Read more" links now point at these pages (Metal Roofing/Repairs/Storm → roofing, Siding → siding, Concrete & Framing → concrete, Commercial & Ag → framing). **Not in the nav menu yet** — the Cornerstone `nav-inline` has no dropdown styling; add a sub-menu under Our Services only after styling it. **All copy is drafted — owner must verify claims** (warranty years, gauge, permit handling, post-frame scope).
- **`triple5-services` mu-plugin** — registers a reusable **"Triple 5 Services Grid" Cornerstone element** (`cs_register_element('t5-services', …)`). Centered eyebrow + heading, then a responsive 3-col grid of up to **6 service cards** (fixed slots; empty-title cards are skipped so a placement can show 2–6). Each card: title, description, Read-more link, media **tone** (navy/clay/coal blueprint placeholder), optional real image. Defaults = the 6 Triple 5 service lines. Symlinked in. **Demo placed live on Home** (below the hero + feature: order hero → feature → services).
- **Themeco Pro** installed and active; Cornerstone bundled inside the theme (no separate plugin).
- **Design system** installed as a personal Claude skill at `~/.claude/skills/triple5-design/` (`/triple5-design`).

### Recent Focus Areas (last ~5 sessions)
1. **Home service-panel copy + links (2026-09-18):** rewrote the red card + six tile descriptions to match the real service lines and linked every tile to its service page; added the linked-tile hover affordance in `panel.css`. Verified click-through in browser.
2. **Five service pages (2026-09-18):** roofing / siding / remodeling / concrete / framing built as children of Our Services from existing elements (see What's Working); `t5-tabs` gained single-tab mode (`is-single`, no bar). Verified live. Bake gotcha held for brand-new *documents* too — every first pass baked empty, second pass fine.
3. **Service Tabs element + Our Services page (2026-09-18):** `triple5-tabs` built and placed as the whole `/our-services/` page; tab switching, deep links and no-JS stacking verified. **Bake gotcha refined:** a brand-new element type bakes EMPTY in the *PHP process* where it is first seen (two `updateElements` calls in one `wp eval` don't help) — run a second `wp eval` that calls `updateElements` again, then confirm with `substr_count(post_content, 't5-<name>')`. Front-end always renders from data, so the empty bake is invisible in the browser.
4. **FAQ element (2026-09-18):** `triple5-faq` built, placed on Home after testimonials, verified live (accordion toggles, schema emitted).
5. **Testimonials element (2026-09-18):** `triple5-reviews` built, placed on Home, verified (see What's Working). Next step for the owner: add Google Place ID + API key (and optionally Yelp) on Settings → Triple 5 Reviews, click Refresh, then replace/disable the manual sample reviews on the element. **Gotcha learned:** when placing elements via WP-CLI, do NOT call `do_action('cs_register_elements')` manually before Cornerstone boots — it double-fires and the new element bakes EMPTY into post_content (front-end still rendered from data, which masked it). Just call `updateElements` and let Cornerstone register.
6. **Two new Home elements (2026-09-18):** `triple5-panel` (Service Panel, after the hero) and `triple5-why` (Why Choose Us, after the About section) — both from owner reference screenshots, both root-placed for full-bleed dark bands, both verified live in the browser. Learned: pages must be re-baked with `updateElements(...)`, not `update()->save()`; `cornerstone('Resolver')` caches document instances within a request, so read `_cornerstone_data` meta to confirm a save. Element-library pattern is now well-worn: `cs_register_element` + fixed-slot skip-empty items + `select` icon picker over an inline Lucide path map.

### Header approach (decided)
Header is the **native Cornerstone header** (ID 26), editable in Pro → Headers. The coded `triple5-header` mu-plugin was the first attempt and is **disabled** (kept for reference/rollback). Pro's default chrome is removed per page via `template-blank-5.php`. **No footer exists yet** on any page.

### Known Issues / Tech Debt
- **Logo strip band is not full-width** (ends ~2/3 across at 1512px) — pre-existing layout issue in `triple5-logos` placement/CSS, noticed 2026-09-15.
- **Pro license not validated** — updates/template library/extensions locked until you enter the Themeco key in wp-admin (Pro/Cornerstone → Validation). Interactive, browser-only.
- **Pro global color/font pickers** in the Cornerstone UI still need to be set to match the brand tokens (interactive). The mu-plugin gives a CSS baseline; the builder's own swatches are separate.
- **Testimonials show sample reviews** (4 hand-typed cards from the reference mockup, labelled as Google) until real API credentials are added — these are NOT real customer reviews; replace before launch.
- Substituted assets in the design system: Google Fonts (not licensed brand fonts), Lucide icons, placeholder imagery. Real job photos are the top asset need.
- `triple5` block theme is a minimal starter, not a finished design.

---

## Key Architectural Decisions

- **Repo is the source of truth; Local site holds symlinks.** Git can't version-control contents through a symlink pointing the other way, so real files live here and are symlinked into `~/Local Sites/triple5/app/public/wp-content/`. This gives both version control and live edits.
- **Custom code only in git.** `.gitignore` excludes WP core, default themes, uploads. Only `wp-content/themes/triple5/` and the `wp-content/mu-plugins/triple5-*` plugins are tracked.
- **Brand delivered as a must-use plugin**, not baked into the theme — so it applies regardless of active theme (currently Pro) and is trivially reversible.
- **WP-CLI** runs from `~/Local Sites/triple5/app/public` using Local's bundled PHP (`~/Library/Application Support/Local/lightning-services/php-8.2.29+0/bin/darwin-arm64/bin/php`) + `wp-cli.phar` from `/Applications/Local.app/Contents/Resources/extraResources/bin/wp-cli/`, with `-d mysqli.default_socket=~/Library/Application Support/Local/run/SCbtevTBr/mysql/mysqld.sock` (site ID `SCbtevTBr`). `wp` is NOT on PATH — build a wrapper script. Site must be running in Local first.

---

## Conventions

- Keep this file updated continuously during work sessions.
- Design decisions go in DESIGN_CONTEXT.md (auto-loads on UI work), not here.
- Commit/push only when the user asks.
